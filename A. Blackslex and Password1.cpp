    #include<bits/stdc++.h>
    using namespace std;
    #define INI cin.tie(0)->sync_with_stdio(0);cout.tie(0);
    #define read1(a) int a;cin>>a;
    #define fore(i,a,b) for(int i=a;i<=b;i++)
    #define forei(i,a,b) for(int i=a;i>=b;i--)
    #define read2(a,b) int a,b;cin>>a>>b;
    #define read3(a,b,c) int a, b,c; cin>>a>>b>>c;
    #define read4(a,b,c,d) int a,b,c,d;cin>>a>>b>>c>>d;
    #define max3(a,b,c) max(a,max(b,c))
    #define max4(a,b,c,d) max(a,max3(b,c,d))
    #define ii pair<int,int>
    #define vi vector<int>
    #define vii vector<ii>
    #define viii vector<int,ii>
    #define readv(vec,a) read1(a) vi vec(a);for(auto &e:vec)cin>>e;
    #define printv(vec) for(auto e:vec)cout<<e<<' ';cout<<endl;
    #define vs vector<string>
    #define endl '\n';
    #define readst(x) string x;cin>>x;
    #define HW cout<<"Hola Mundo";
    #define Y cout<<"Yes";
    #define N cout<<"No";
    #define aureo (1+sqrt(5))/2
    #define sum(n) (n)*((n+1))/2
    #define spin(J) (pow(aureo,n)-pow(1-aureo),n))/(sqrt(5))
    #define query read1(a) while (a--)
    typedef long long ll;
    typedef unsigned long long ull;
    typedef double long dl;
    const ll inf=numeric_limits<ll>::max();
    const dl PI=acos(-1);
    const ll MOD=1e9+7;
    void solve()
    {
        read3(a,b,c)

    }
    int main()
    {
        INI query solve();
    }
